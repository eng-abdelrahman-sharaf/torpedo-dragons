#include "system.h"

int main() {
	System sys;
	sys.mainLoop();
    // Logger *logger = Logger::init("logging.txt");
    // Customer customer("1", "ali");
    // logger->logPurchase(&customer , 120);
}